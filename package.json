{
	"name": "fragmento",
	"description": "A project by platdesign.de",
	"private": true,
	"author": [
		{
			"name": "Christian Blaschke",
			"email": "mail@platdesign.de",
			"url": "https://github.com/platdesign"
		}
	],
	"version": "0.0.0",
	"repository": "",
	"devDependencies": {
		"@hapi/code": "^8.0.2",
		"lerna": "^3.22.1",
		"mocha": "^8.2.0"
	},
	"scripts": {
		"lerna": "lerna",
		"bootstrap": "lerna bootstrap",
		"publish": "npm run bootstrap && npm run test && lerna publish --force-publish=*",
		"test:w": "lerna run test:w --parallel",
		"test": "lerna run test --parallel",
		"outdated": "lerna exec npm outdated --no-bail",
		"update:s": "lerna exec npm update -- -s"
	},
	"dependencies": {}
}
